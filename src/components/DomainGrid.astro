---
import { t, type Lang } from "../i18n/ui";
interface Props { lang: Lang }
const { lang } = Astro.props;

const domains = [
  { icon: "⚡", title: t(lang, "domains.energy.title"), description: t(lang, "domains.energy.desc") },
  { icon: "🏭", title: t(lang, "domains.industrial.title"), description: t(lang, "domains.industrial.desc") },
  { icon: "📊", title: t(lang, "domains.finance.title"), description: t(lang, "domains.finance.desc") },
  { icon: "🏢", title: t(lang, "domains.business.title"), description: t(lang, "domains.business.desc") },
  { icon: "🌍", title: t(lang, "domains.environmental.title"), description: t(lang, "domains.environmental.desc") },
  { icon: "🧬", title: t(lang, "domains.biomedical.title"), description: t(lang, "domains.biomedical.desc") },
];
---

<section id="domains" class="py-20 sm:py-28">
  <div class="mx-auto max-w-5xl px-6">
    <h2 class="text-3xl sm:text-4xl font-bold text-barivia-dark text-center tracking-tight">
      {t(lang, "domains.heading")}
    </h2>
    <p class="mt-4 text-center text-barivia-gray text-lg max-w-2xl mx-auto">
      {t(lang, "domains.sub")}
    </p>
    <div class="mt-14 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {domains.map((domain) => (
        <div class="rounded-xl bg-white p-6 shadow-sm border border-gray-100 hover:shadow-md transition-shadow">
          <div class="text-3xl mb-3">{domain.icon}</div>
          <h3 class="text-lg font-semibold text-barivia-dark">{domain.title}</h3>
          <p class="mt-2 text-barivia-gray leading-relaxed text-sm">{domain.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
